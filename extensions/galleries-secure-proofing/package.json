{
  "name": "@michaelthielemann/kestrel-galleries-secure-proofing",
  "version": "2.0.0",
  "description": "Opt-in Kestrel extension: client proofing (Lightroom-style colour flags + comments) on top of kestrel-galleries-secure. Customer marks are encrypted client-side; a public back-channel stores only ciphertext. Requires a running server (not pure-static).",
  "type": "module",
  "main": "./nuxt.config.ts",
  "//publish": "Opt-in extension LAYER that EXTENDS kestrel-galleries-secure. Composed AFTER both: extends: ['kestrel', 'kestrel-galleries-secure', 'kestrel-galleries-secure-proofing']. Ships a public /api back-channel + a proofing collection + customer/photographer views — needs a running Node server (NOT the pure-static enterprise deployment). To publish: drop `private`, then `npm publish`.",
  "files": [
    "NOTICE",
    "nuxt.config.ts",
    "server",
    "app",
    "!**/*.test.ts",
    "!**/*.dom.test.ts",
    "!**/*.nuxt.test.ts"
  ],
  "peerDependencies": {
    "@michaelthielemann/kestrel": "*",
    "@michaelthielemann/kestrel-galleries-secure": "*",
    "drizzle-orm": "^0.45.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@michaelthielemann/kestrel": "workspace:*",
    "@michaelthielemann/kestrel-galleries-secure": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "vue": "3.5.35"
  },
  "license": "Apache-2.0",
  "author": "Michael Thielemann <283621694+MichaelThielemann@users.noreply.github.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MichaelThielemann/kestrel.git",
    "directory": "extensions/galleries-secure-proofing"
  },
  "bugs": "https://github.com/MichaelThielemann/kestrel/issues",
  "publishConfig": {
    "access": "public",
    "provenance": true
  }
}
