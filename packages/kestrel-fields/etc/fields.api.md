## API Report File for "@kestrel/fields"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { allBlocks } from '@kestrel/core';
import type { BlockDef } from '@kestrel/core';
import { buildBlocksSchema } from '@kestrel/core';
import { clearBlocks } from '@kestrel/core';
import type { FieldDef } from '@kestrel/core';
import type { FieldPopulator } from '@kestrel/core';
import type { FieldType } from '@kestrel/core';
import type { FieldTypeDescriptor } from '@kestrel/core';
import { getBlock } from '@kestrel/core';
import type { KeyMode } from '@kestrel/core';
import type { PipelineDef } from '@kestrel/core';
import type { PopulateCtx } from '@kestrel/core';
import type { Populator } from '@kestrel/core';
import { registerBlock } from '@kestrel/core';
import type { SQLiteColumnBuilder } from 'drizzle-orm/sqlite-core';
import { z } from 'zod';

export { allBlocks }

// @public
export type ApplyBlockFields = (props: Record<string, unknown>, fields: Record<string, FieldDef>) => void;

// @public
export function applyFieldPopulators(bag: Record<string, unknown>, fields: Record<string, FieldDef>, keyMode: KeyMode, ctx: PopulateCtx, lookup: PopulatorLookup): void;

export { BlockDef }

// @public
export function buildBlockPopulator(applyFields: ApplyBlockFields): (nodes: unknown) => unknown[];

// @public
export function buildBlocksPipelines(): PipelineDef[];

export { buildBlocksSchema }

// @public
export function buildFieldTreePopulator(lookup?: PopulatorLookup): Populator;

export { clearBlocks }

// @public
export function constrain<T extends SQLiteColumnBuilder>(col: T, field: FieldDef): T;

// @public
export function defineBlock(def: BlockDef): BlockDef;

// @public
export function defineFieldType(def: FieldTypeDef): FieldTypeDescriptor;

// @public
export interface FieldTypeDef extends FieldTypeDescriptor {
    // (undocumented)
    name: string;
}

export { FieldTypeDescriptor }

// @public
export const fieldTypes: Record<string, FieldTypeDescriptor<FieldType>>;

export { getBlock }

// @public
export const opt: (s: z.ZodType, field: FieldDef) => z.ZodType;

// @public
export const optArr: (s: z.ZodType, field?: FieldDef) => z.ZodType;

// @public
export type PopulatorLookup = (type: string) => FieldPopulator | undefined;

export { registerBlock }

```
