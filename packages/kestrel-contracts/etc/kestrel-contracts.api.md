## API Report File for "@michaelthielemann/kestrel-contracts"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Schema } from 'effect';

// @public
export type AdapterContract<T> = {
    readonly [K in keyof T]: T[K] extends (...args: never[]) => Promise<infer R> ? Schema.Schema<R> : T[K] extends (...args: never[]) => infer R ? Schema.Schema<R> : never;
};

// @public
export function clearUpcasts(): void;

// Warning: (ae-forgotten-export) The symbol "Conflict_base" needs to be exported by the entry point index.d.ts
//
// @public
export class Conflict extends Conflict_base {
}

// @public
export const ConflictDetails: Schema.Union<[Schema.Struct<{
    kind: Schema.Literal<["duplicate"]>;
    suggestion: Schema.optional<typeof Schema.String>;
    existingId: Schema.optional<typeof Schema.Int>;
}>, Schema.Struct<{
    kind: Schema.Literal<["stale"]>;
    expectedUpdatedAt: typeof Schema.String;
    actualUpdatedAt: typeof Schema.String;
}>]>;

// @public (undocumented)
export type ConflictDetails = Schema.Schema.Type<typeof ConflictDetails>;

// @public
export const CONTRACTS_VERSION = "0.1.0";

// @public
export interface DeleteOptions {
    // (undocumented)
    readonly pruneEmptyDirs?: boolean;
}

// @public
export interface DeliveryPort {
    publishSnapshot(s: PublishedSnapshot): Promise<void>;
    rebuildAll(iter: AsyncIterable<PublishedSnapshot>): Promise<void>;
    removeRoutes(routes: string[]): Promise<void>;
}

// Warning: (ae-forgotten-export) The symbol "DuplicateUpcast_base" needs to be exported by the entry point index.d.ts
//
// @public
export class DuplicateUpcast extends DuplicateUpcast_base {
}

// @public
export const EscapedHtml: Schema.brand<typeof Schema.String, "Escaped">;

// @public
export type EscapedHtml = Schema.Schema.Type<typeof EscapedHtml>;

// @public
export const EventEnvelope: Schema.Struct<{
    id: typeof Schema.UUID;
    name: typeof Schema.String;
    version: typeof Schema.Int;
    aggregate: Schema.Struct<{
        collection: typeof Schema.String;
        recordId: typeof Schema.Int;
    }>;
    sequence: typeof Schema.Int;
    correlationId: typeof Schema.String;
    causation: Schema.Struct<{
        pipeline: typeof Schema.String;
        op: typeof Schema.String;
    }>;
    occurredAt: typeof Schema.DateTimeUtc;
    payload: typeof Schema.Unknown;
}>;

// @public
export type EventEnvelope = Schema.Schema.Type<typeof EventEnvelope>;

// Warning: (ae-forgotten-export) The symbol "Forbidden_base" needs to be exported by the entry point index.d.ts
//
// @public
export class Forbidden extends Forbidden_base {
}

// @public
export interface IdentityProviderAdapter {
    verifyCredentials(credentials: {
        readonly password: string;
    }): Promise<IdentityVerification>;
}

// @public
export const IdentityProviderAdapterSchemas: AdapterContract<IdentityProviderAdapter>;

// @public
export type IdentityVerification = Schema.Schema.Type<typeof IdentityVerificationSchema>;

// @public
export const IdentityVerificationSchema: Schema.Union<[Schema.Struct<{
    ok: Schema.Literal<[true]>;
    subject: typeof Schema.String;
}>, Schema.Struct<{
    ok: Schema.Literal<[false]>;
}>]>;

// @public
export type KestrelError = NotFound | Forbidden | Unauthorized | Conflict | ValidationFailed | Locked | Quarantined;

// Warning: (ae-forgotten-export) The symbol "Locked_base" needs to be exported by the entry point index.d.ts
//
// @public
export class Locked extends Locked_base {
}

// @public
export interface MediaStorageAdapter extends StorageAdapter {
    copy(srcKey: string, dstKey: string): Promise<void>;
    ensureDir(folder: string): Promise<void>;
    get(key: string): Promise<Uint8Array>;
    listPrefix(prefix: string): Promise<readonly string[]>;
    removeDir(folder: string): Promise<void>;
    stat(key: string): Promise<StorageObjectStat | null>;
}

// @public
export const MediaStorageAdapterSchemas: AdapterContract<MediaStorageAdapter>;

// Warning: (ae-forgotten-export) The symbol "NotFound_base" needs to be exported by the entry point index.d.ts
//
// @public
export class NotFound extends NotFound_base {
}

// @public
export const OwnershipManifest: Schema.Struct<{
    module: typeof Schema.String;
    tables: Schema.Array$<typeof Schema.String>;
}>;

// @public
export type OwnershipManifest = Schema.Schema.Type<typeof OwnershipManifest>;

// @public
export const PublishedSnapshot: Schema.brand<Schema.Struct<{
    route: typeof Schema.String;
    locale: Schema.NullOr<typeof Schema.String>;
    html: typeof Schema.String;
    media: Schema.Array$<typeof Schema.String>;
    fingerprint: typeof Schema.String;
    publishedAt: typeof Schema.Number;
}>, "Snapshot">;

// @public
export type PublishedSnapshot = Schema.Schema.Type<typeof PublishedSnapshot>;

// @public
export interface PutOptions {
    // (undocumented)
    readonly cacheControl?: string;
    // (undocumented)
    readonly contentEncoding?: string;
}

// Warning: (ae-forgotten-export) The symbol "Quarantined_base" needs to be exported by the entry point index.d.ts
//
// @public
export class Quarantined extends Quarantined_base {
}

// @public
export function registerUpcast(name: string, fromVersion: number, fn: UpcastFn): void;

// @public
export const ResolvedSlug: Schema.brand<typeof Schema.String, "Slug">;

// @public
export type ResolvedSlug = Schema.Schema.Type<typeof ResolvedSlug>;

// @public
export const SanitizedRichtext: Schema.brand<typeof Schema.String, "Sanitized">;

// @public
export type SanitizedRichtext = Schema.Schema.Type<typeof SanitizedRichtext>;

// @public
export interface StorageAdapter {
    delete(key: string, opts?: DeleteOptions): Promise<void>;
    exists(key: string): Promise<boolean>;
    list(): Promise<readonly string[]>;
    publicUrl(key: string): string;
    put(key: string, bytes: Uint8Array, contentType: string, opts?: PutOptions): Promise<void>;
}

// @public
export const StorageAdapterSchemas: AdapterContract<StorageAdapter>;

// @public
export interface StorageObjectStat {
    // (undocumented)
    readonly mtimeMs: number | null;
}

// Warning: (ae-forgotten-export) The symbol "Unauthorized_base" needs to be exported by the entry point index.d.ts
//
// @public
export class Unauthorized extends Unauthorized_base {
}

// @public
export type UpcastError = DuplicateUpcast | UpcastGap;

// @public
export type UpcastFn = (payload: unknown) => unknown;

// Warning: (ae-forgotten-export) The symbol "UpcastGap_base" needs to be exported by the entry point index.d.ts
//
// @public
export class UpcastGap extends UpcastGap_base {
}

// @public
export function upcastToLatest(event: EventEnvelope): EventEnvelope;

// @public
export const ValidatedInput: Schema.brand<Schema.mutable<Schema.Record$<typeof Schema.String, typeof Schema.Unknown>>, "Validated">;

// @public
export type ValidatedInput = Schema.Schema.Type<typeof ValidatedInput>;

// Warning: (ae-forgotten-export) The symbol "ValidationFailed_base" needs to be exported by the entry point index.d.ts
//
// @public
export class ValidationFailed extends ValidationFailed_base {
}

```
