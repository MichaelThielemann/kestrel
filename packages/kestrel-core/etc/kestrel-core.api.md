## API Report File for "@michaelthielemann/kestrel-core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { AnySQLiteColumn } from 'drizzle-orm/sqlite-core';
import type { AnySQLiteTable } from 'drizzle-orm/sqlite-core';
import { BetterSQLite3Database } from 'drizzle-orm/better-sqlite3';
import { Context } from 'effect';
import type Database from 'better-sqlite3';
import { Effect } from 'effect';
import { EventEnvelope } from '@michaelthielemann/kestrel-contracts';
import { H3Event } from 'h3';
import type { KestrelError } from '@michaelthielemann/kestrel-contracts';
import { Layer } from 'effect';
import type { OwnershipManifest } from '@michaelthielemann/kestrel-contracts';
import { SanitizedRichtext } from '@michaelthielemann/kestrel-contracts';
import sanitizeHtml from 'sanitize-html';
import type { Schema } from 'effect';
import { SQL } from 'drizzle-orm';
import { SQLiteColumn } from 'drizzle-orm/sqlite-core';
import type { SQLiteColumnBuilderBase } from 'drizzle-orm/sqlite-core';
import { SQLiteTable } from 'drizzle-orm/sqlite-core';
import { SQLiteTableWithColumns } from 'drizzle-orm/sqlite-core';
import { ValidationFailed } from '@michaelthielemann/kestrel-contracts';
import { VoidIfEmpty } from 'effect/Types';
import { YieldableError } from 'effect/Cause';
import { z } from 'zod';
import type { ZodType } from 'zod';

// @public
export interface AccessSpec {
    // (undocumented)
    public?: boolean;
    resource?: string;
    // (undocumented)
    role?: string;
    // Warning: (ae-forgotten-export) The symbol "ReadScope" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    scope?: ReadScope;
}

// @public
export function aggregateKeyOf(aggregate: {
    collection: string;
    recordId: number;
}): string;

// @public (undocumented)
export function allBlocks(allowed?: string[]): BlockDef[];

// @public (undocumented)
export function allCollections(): BuiltCollection[];

// Warning: (ae-forgotten-export) The symbol "Row_2" needs to be exported by the entry point index.d.ts
//
// @public
export function applyFieldTransforms(c: BuiltCollection, values: Row_2, record: Row_2, all: boolean): void;

// @public
export function applyRevisionUpcast(def: CollectionDef, target: RevisionRow): RevisionUpcastOutcome;

// @public
export function assertBodyLimit(contentLength: string | undefined, maxBytes: number): void;

// Warning: (ae-forgotten-export) The symbol "StepFn" needs to be exported by the entry point index.d.ts
// Warning: (ae-forgotten-export) The symbol "StepOptions" needs to be exported by the entry point index.d.ts
//
// @public
export function asyncStep(name: string, fn: StepFn, options?: StepOptions): StepDef;

// @public (undocumented)
export interface BatchResult {
    // (undocumented)
    count: number;
    // (undocumented)
    ids: number[];
}

// @public (undocumented)
export interface Block {
    // (undocumented)
    id: string;
    // (undocumented)
    props: Record<string, unknown>;
    // (undocumented)
    slots?: Record<string, Block[]>;
    // (undocumented)
    type: string;
}

// @public (undocumented)
export interface BlockDef {
    // (undocumented)
    fields: Record<string, FieldDef>;
    icon?: string;
    image?: string;
    // (undocumented)
    label?: Localized;
    // (undocumented)
    name: string;
    // (undocumented)
    slots?: string[];
}

// @public
export interface BrokenRef {
    // (undocumented)
    reason: DeadReason;
    // (undocumented)
    source: FieldRef;
    // (undocumented)
    target: FieldRef;
}

// @public (undocumented)
export function buildBlocksSchema(blockDefs: BlockDef[]): z.ZodType;

// @public (undocumented)
export function buildCollection(def: CollectionDef): BuiltCollection;

// @public
export function buildCollectionActions(collectionName: string): SerializedAction[];

// @public
export function buildContentManifest(extra?: BuiltCollection): OwnershipManifest;

// @public
export function buildEnvelope(input: {
    name: string;
    version: number;
    aggregate: {
        collection: string;
        recordId: number;
    };
    sequence: number;
    correlationId: string;
    causation: {
        pipeline: string;
        op: string;
    };
    occurredAt: string;
    payload: unknown;
}): EventEnvelope;

// @public
export function buildFieldObjectSchema(fields: Record<string, FieldDef>): Record<string, ZodType>;

// @public
export function buildFieldSchema(fields: Record<string, FieldDef>): Record<string, ZodType>;

// @public
export function buildIntrospectionPipelines(): PipelineDef[];

// @public
export function buildOpenApiDocument(): Record<string, unknown>;

// @public
export function buildOpenApiPipelines(): PipelineDef[];

// @public
export function buildOutboxPipelines(): PipelineDef[];

// @public
export function buildPipelineIndex(): PipelineDescriptor[];

// @public (undocumented)
export function buildTable(def: CollectionDef): SQLiteTable;

// @public
export function buildToolingPipelines(): PipelineDef[];

// @public (undocumented)
export interface BuiltCollection {
    applyConditions?: (record: Record<string, unknown>) => {
        issues: ConditionIssue[];
    };
    // (undocumented)
    def: CollectionDef;
    // (undocumented)
    insert: CollectionSchema;
    // (undocumented)
    name: string;
    // (undocumented)
    select: CollectionSchema;
    // (undocumented)
    table: SQLiteTable;
    // (undocumented)
    update: CollectionSchema;
}

// @public
export function cacheControlFor(key: string): string | undefined;

// @public
export function canTransition(from: Status, to: Status, guardResults: Partial<Record<GuardName, boolean>>): boolean;

// @public
export function captureRead(coll: string, id?: number | null): void;

// @public
export function clampPerPage(n: unknown): number;

// @public (undocumented)
export function clearBlocks(): void;

// @public (undocumented)
export function clearFieldPopulators(): void;

// @public
export function clearOutboxHandlers(): void;

// @public (undocumented)
export function clearPipelines(): void;

// @public (undocumented)
export function clearPopulator(): void;

// @public
export function clearPruneCursors(): void;

// @public (undocumented)
export function clearRegistry(): void;

// @public
export function clearResolvedKestrelConfig(): void;

// @public
export function clearRevisionUpcasts(): void;

// @public
export function coerceFilterValue(col: {
    getSQLType(): string;
    mode?: string;
}, value: unknown): unknown;

// @public
export interface CollectionDef {
    // (undocumented)
    blocks?: {
        enabled: true;
        allowed?: string[];
    };
    builtin?: boolean;
    editor?: string;
    fieldLayout?: FieldLayoutDSL;
    // (undocumented)
    fields: Record<string, FieldDef>;
    icon?: string;
    label?: {
        singular?: Localized;
        plural?: Localized;
        new?: Localized;
    };
    // (undocumented)
    mode: 'multi' | 'single';
    // (undocumented)
    name: string;
    nav?: boolean;
    // (undocumented)
    pageLike?: boolean;
    // (undocumented)
    seo?: boolean;
    // (undocumented)
    status?: boolean;
    translatable?: boolean;
    validate?: (record: Record<string, unknown>) => Array<{
        path: (string | number)[];
        message: string;
    }>;
}

// @public
export function collectionEnabled(def: CollectionDef, toggles?: Record<string, boolean>): boolean;

// @public
export function collectionMayReference(def: CollectionDef): boolean;

// @public (undocumented)
export function collectionOf(ctx: PipelineContext): BuiltCollection;

// @public
export type CollectionSchema = z.ZodObject<z.ZodRawShape>;

// @public
export function collectRichtextRefs(html: string | null | undefined): {
    collection: string;
    id: number;
}[];

// @public (undocumented)
export interface ColumnShape {
    // (undocumented)
    autoIncrement: boolean;
    default: string | null;
    // (undocumented)
    name: string;
    // (undocumented)
    notNull: boolean;
    // (undocumented)
    primaryKey: boolean;
    renamedFrom?: string;
    renameTransform?: 'wrap' | 'unwrap';
    type: string;
}

// @public
export type Condition = ConditionRule | {
    and: Condition[];
} | {
    or: Condition[];
} | {
    not: Condition;
};

// @public (undocumented)
export interface ConditionIssue {
    // (undocumented)
    message: string;
    // (undocumented)
    path: (string | number)[];
}

// @public
export interface ConditionOperator {
    empty?: boolean;
    // (undocumented)
    eq?: ConditionScalar;
    // (undocumented)
    gt?: number | string;
    // (undocumented)
    gte?: number | string;
    // (undocumented)
    in?: ConditionScalar[];
    // (undocumented)
    lt?: number | string;
    // (undocumented)
    lte?: number | string;
    // (undocumented)
    ne?: ConditionScalar;
    // (undocumented)
    notIn?: ConditionScalar[];
    regexp?: string;
}

// @public
export interface ConditionRule {
    // (undocumented)
    field: string;
    // (undocumented)
    is?: ConditionScalar;
    // (undocumented)
    op?: ConditionOperator;
}

// @public
export type ConditionScalar = string | number | boolean | null;

// @public
export type ContentDb = ModuleDbService['db'];

// @public
export function contentTables(extra?: BuiltCollection): Record<string, AnySQLiteTable>;

// @public
export function contentTypeFor(filename: string, hasEncoding?: boolean): string;

// Warning: (ae-forgotten-export) The symbol "DB_2" needs to be exported by the entry point index.d.ts
// Warning: (ae-forgotten-export) The symbol "Row_3" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function create(db: DB_2, c: BuiltCollection, body: unknown): Row_3;

// @public
export function createLocalDriver(opts: {
    dir: string;
    baseUrl: string;
}): StorageDriver;

// Warning: (ae-forgotten-export) The symbol "PipelineContextOptions" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function createPipelineContext<TIn = unknown, TOut = unknown>(options: PipelineContextOptions): PipelineContext<TIn, TOut>;

// @public
export function createS3Driver(opts: S3DriverOptions, fetchImpl?: typeof fetch): StorageDriver;

// @public
export interface DashboardCollectionSummary {
    // (undocumented)
    fieldCount: number;
    // (undocumented)
    mode: string;
    // (undocumented)
    name: string;
    // (undocumented)
    translatable: boolean;
}

// @public
export interface DashboardData {
    // (undocumented)
    collections?: DashboardCollectionSummary[];
    generatedAt?: string;
    // (undocumented)
    graph?: DashboardGraphSection;
    // (undocumented)
    pipelines: PipelineDescriptor[];
    // (undocumented)
    pluginOrder?: DashboardPluginOrderEntry[];
}

// @public
export interface DashboardGraphSection {
    // (undocumented)
    apiSurface?: {
        report: string;
        count: number;
        ceiling: number;
    }[];
    // (undocumented)
    packageEdges?: {
        from: string;
        to: string;
        debt?: boolean;
    }[];
}

// @public
export interface DashboardPluginOrderEntry {
    // (undocumented)
    after: string[];
    // (undocumented)
    file: string;
    // (undocumented)
    layer: string;
}

// Warning: (ae-forgotten-export) The symbol "DB" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function dbOf(ctx: PipelineContext): DB;

// @public
export type DeadReason = 'missing' | 'unpublished';

// Warning: (ae-forgotten-export) The symbol "DB_4" needs to be exported by the entry point index.d.ts
//
// @public
export function deadTargets(db: DB_4, targets: FieldRef[]): Map<string, DeadReason>;

// @public (undocumented)
export const DEFAULT_IMAGE_POLICY: ResolvedImagePolicy;

// @public
export const DEFAULT_OP: Record<FilterKind, FilterOp>;

// @public
export const DEFAULT_PER_PAGE = 25;

// @public
export function defaultCollectionOps(): string[];

// @public
export function defineCollection(def: CollectionDef): CollectionDef;

// @public (undocumented)
export function definePipeline(def: PipelineDef): PipelineDef;

// @public
export interface DeleteOptions {
    pruneEmptyDirs?: boolean;
}

// @public
export function deliveryExemptPrefixes(): string[];

// @public
export function deriveLocatedDeadRefs(db: DB_4, def: CollectionDef, row: Record<string, unknown>): LocatedDeadRef[];

// @public
export function describeOp(op: SchemaOp): string;

// @public
export function desiredFromCollections(collections: (CollectionDef | BuiltCollection)[], opts?: DesiredOptions): SchemaSnapshot;

// @public (undocumented)
export interface DesiredOptions {
    extraTables?: AnySQLiteTable[];
    toggles?: Record<string, boolean>;
}

// @public
export function desiredSchema(tables: SQLiteTable[], defsByName?: Map<string, CollectionDef>): SchemaSnapshot;

// @public (undocumented)
export interface Dialect {
    introspect(db: IntrospectDb): SchemaSnapshot;
    readonly name: string;
    quote(id: string): string;
    render(ops: SchemaOp[]): string[];
}

// @public
export function diffSchema(desired: SchemaSnapshot, actual: SchemaSnapshot): SchemaOp[];

// @public
export function ensureBuilt(c: CollectionDef | BuiltCollection): BuiltCollection;

// @public
export function ensureOutboxTable(sqlite: Database.Database, module: string): void;

// @public
export function ensureRevisionsTable(sqlite: Database.Database, collection: string): void;

// @public
export const escapeLike: (s: string) => string;

// @public
export function evaluateCondition(condition: Condition, scope: Record<string, unknown>): boolean;

// @public
export function eventOf(ctx: PipelineContext): H3Event;

// @public
export function eventsOf(ctx: PipelineContext): WriteEvent[];

// @public
export function extractFieldRefs(fields: Record<string, FieldDef>, bag: Record<string, unknown>, keyMode: KeyMode): FieldRef[];

// @public
export function extractLocatedRecordRefs(def: CollectionDef, row: Record<string, unknown>): LocatedRef[];

// @public
export function extractRecordRefs(def: CollectionDef, row: Record<string, unknown>): FieldRef[];

// @public
export function fieldCanEnforceUnique(field: FieldDef): boolean;

// Warning: (ae-forgotten-export) The symbol "BaseFieldDef" needs to be exported by the entry point index.d.ts
//
// @public
export type FieldDef = (BaseFieldDef & {
    type: 'text';
    options?: {
        minLength?: number;
        maxLength?: number;
        multiline?: boolean;
    };
}) | (BaseFieldDef & {
    type: 'slug';
    options?: {
        from?: string;
        prefix?: string;
    };
}) | (BaseFieldDef & {
    type: 'richtext';
}) | (BaseFieldDef & {
    type: 'number';
    options?: {
        min?: number;
        max?: number;
        integer?: boolean;
        decimals?: number;
        unit?: string;
        units?: string[];
    };
}) | (BaseFieldDef & {
    type: 'boolean';
}) | (BaseFieldDef & {
    type: 'datetime';
    options?: {
        precision?: 'date' | 'datetime' | 'time';
        range?: boolean;
    };
}) | (BaseFieldDef & {
    type: 'choice';
    options: {
        choices: {
            label: Localized;
            value: string;
        }[];
        multiple?: boolean;
        display?: 'select' | 'buttons' | 'checkboxes';
    };
}) | (BaseFieldDef & {
    type: 'link';
    options?: {
        types?: LinkType[];
        collections?: string[];
    };
}) | (BaseFieldDef & {
    type: 'media';
    options?: {
        multiple?: boolean;
        accept?: 'image' | 'any';
    };
}) | (BaseFieldDef & {
    type: 'relation';
    relation: {
        collection: string;
        many?: boolean;
        labelField?: string;
    };
}) | (BaseFieldDef & {
    type: 'repeater';
    options: {
        fields: Record<string, FieldDef>;
        fieldLayout?: FieldLayoutDSL;
    };
}) | (BaseFieldDef & {
    type: 'json';
}) | (BaseFieldDef & {
    type: string & {};
    options?: Record<string, unknown>;
});

// @public
export function fieldFilterKind(f: SerializedField): FilterKind | null;

// @public
export function fieldIs<T extends FieldType>(field: FieldDef, type: T): field is FieldOf<T>;

// @public
export type FieldLayoutDSL = FieldLayoutEntry[];

// @public
export type FieldLayoutEntry = string | string[] | Record<string, FieldLayoutEntry[]>;

// @public
export type FieldOf<T extends FieldType> = [Extract<FieldDef, {
    type: T;
}>] extends [never] ? FieldDef : Extract<FieldDef, {
    type: T;
}>;

// @public
export type FieldPopulator = (bag: Record<string, unknown>, key: string, field: FieldDef, ctx: PopulateCtx, keyMode: KeyMode) => void;

// @public
export interface FieldRef {
    // (undocumented)
    collection: string;
    // (undocumented)
    id: number;
}

// @public
export type FieldType = 'text' | 'slug' | 'richtext' | 'number' | 'boolean' | 'datetime' | 'choice' | 'link' | 'media' | 'relation' | 'repeater' | 'json' | (string & {});

// @public
export interface FieldTypeDescriptor<T extends FieldType = FieldType> {
    column(dbName: string, field: FieldOf<T>): SQLiteColumnBuilderBase;
    transform?(value: unknown, record: Record<string, unknown>, field: FieldOf<T>): unknown;
    validator(field: FieldOf<T>): ZodType;
}

// @public
export const fieldTypes: Record<string, FieldTypeDescriptor>;

// @public
export const FILTER_RE: RegExp;

// @public
export interface FilterClause {
    // (undocumented)
    field: string;
    // (undocumented)
    op: FilterOp;
    // (undocumented)
    value: string;
}

// @public
export function filterCondition(col: AnySQLiteColumn, kind: FilterKind, op: FilterOp, raw: string): SQL;

// @public
export type FilterKind = 'number' | 'text' | 'datetime' | 'boolean' | 'enum' | 'ref' | 'richtext' | 'stringSet' | 'idSet';

// @public (undocumented)
export type FilterOp = 'eq' | 'ne' | 'lt' | 'lte' | 'gt' | 'gte' | 'contains' | 'notContains';

// @public
export function findBrokenRefs(db: DB_4): BrokenRef[] | null;

// @public
export function findReferrers(db: DB_4, targetColl: string, targetId: number): FieldRef[] | null;

// @public
export function findReferrersForMany(db: DB_4, targetColl: string, ids: number[]): Record<number, number> | null;

// @public
export function findRouteConflict(db: BetterSQLite3Database, targetRoute: string, primary: string, collections: BuiltCollection[], exclude: {
    collection: string;
    id: number | null;
}, prefixPrimary?: boolean): RouteConflict | null;

// @public
export function fromThrowing<T>(fn: () => T): Effect.Effect<T, KestrelError>;

// @public
export function fromThrowingAsync<T>(fn: () => Promise<T>): Effect.Effect<T, KestrelError>;

// @public (undocumented)
export interface GateEvaluators {
    // (undocumented)
    access: (spec: AccessSpec, ctx: PipelineContext) => GateOutcome | Promise<GateOutcome>;
    // (undocumented)
    csrf: (ctx: PipelineContext) => GateOutcome | Promise<GateOutcome>;
    // (undocumented)
    ipAllowlist: (ctx: PipelineContext) => GateOutcome | Promise<GateOutcome>;
}

// @public (undocumented)
export interface GateOutcome {
    // (undocumented)
    allowed: boolean;
    // (undocumented)
    detail?: string;
    // (undocumented)
    message?: string;
    // Warning: (ae-forgotten-export) The symbol "RequestFacts" needs to be exported by the entry point index.d.ts
    readScope?: RequestFacts['readScope'];
    status?: 401 | 403;
}

// @public (undocumented)
export function getBlock(name: string): BlockDef | undefined;

// @public (undocumented)
export function getCollection(name: string): BuiltCollection | undefined;

// @public (undocumented)
export function getCollectionOr404(name: string): BuiltCollection;

// @public (undocumented)
export function getFieldPopulator(type: string): FieldPopulator | undefined;

// @public
export function getFieldType(type: string): FieldTypeDescriptor;

// @public (undocumented)
export function getOne(db: DB_2, c: BuiltCollection, id: number, depth?: number, locale?: string, publishedOnly?: boolean, publicOnly?: boolean): Row_3;

// @public
export function getResolvedKestrelConfig(): ResolvedKestrel;

// @public
export function getSingleton(db: DB_2, c: BuiltCollection, locale?: string, publishedOnly?: boolean, depth?: number, publicOnly?: boolean): Row_3 | null;

// @public (undocumented)
export type GuardName = 'conditionsValid';

// @public
export const IF_UNMODIFIED_HEADER = "x-kestrel-if-unmodified-since";

// @public (undocumented)
export interface IndexShape {
    // (undocumented)
    columns: string[];
    // (undocumented)
    name: string;
    // (undocumented)
    table: string;
    // (undocumented)
    unique: boolean;
    where: string | null;
}

// @public
export function insertOutboxRow(sqlite: Database.Database, module: string, envelope: EventEnvelope): void;

// @public
export function insertRevisionRow(sqlite: Database.Database, collection: string, input: {
    recordId: number;
    revision: number;
    snapshot: Row | null;
    schemaVersion: number;
    correlationId: string;
    createdAt: string;
    tombstone?: boolean;
}): void;

// @public (undocumented)
export function introspect(db: IntrospectDb): SchemaSnapshot;

// @public (undocumented)
export interface IntrospectDb {
    // (undocumented)
    pragma(source: string): Row_4[];
    // (undocumented)
    prepare(sql: string): {
        all(...params: unknown[]): Row_4[];
    };
}

// @public
export function isDeliveryExemptPath(path: string): boolean;

// @public
export function isDeliveryLive(): boolean;

// @public
export function isDestructive(op: SchemaOp): boolean;

// @public
export function isEmptyValue(value: unknown): boolean;

// @public
export function isFieldVisible(field: {
    condition?: Condition;
} | null | undefined, scope: Record<string, unknown>): boolean;

// @public
export const isFilterOp: (s: string) => s is FilterOp;

// @public
export function isHardRequired(field: FieldDef): boolean;

// @public
export function isKestrelError(error: unknown): error is KestrelError;

// @public
export function isMetaKey(key: string): boolean;

// @public (undocumented)
export function isRecordId(segment: string): boolean;

// Warning: (ae-forgotten-export) The symbol "ResolvedPipeline" needs to be exported by the entry point index.d.ts
//
// @public
export function isRoutablePipeline(resolved: ResolvedPipeline | undefined): resolved is ResolvedPipeline;

// @public
export function isSingleRefColumn(field: FieldDef): boolean;

// @public (undocumented)
export function isSupportedLocale(value: string): value is Locale;

// @public
export function isUniqueViolation(error: unknown): boolean;

// @public (undocumented)
export interface KestrelConfig {
    aiDisclosure?: {
        enabled?: boolean;
    };
    collections?: {
        pages?: boolean;
        media?: boolean;
    };
    db?: string;
    delivery?: 'static' | 'live';
    deliveryExempt?: string[];
    locales?: string[];
    // (undocumented)
    media?: KestrelMediaConfig;
    output?: {
        driver?: 'local' | 's3';
        dir?: string;
        publicDir?: string;
        auto?: boolean;
        publishOnSave?: boolean;
        reconcileMinutes?: number;
        verbose?: boolean;
        s3?: {
            bucket?: string;
            region?: string;
            endpoint?: string;
            prefix?: string;
        };
    };
    prefixPrimaryLocale?: boolean;
    preview?: {
        desktopWidth?: number;
    };
    // (undocumented)
    primaryLocale?: string;
    revisions?: {
        keep?: number | 'all';
        maxAgeDays?: number;
    };
    seo?: {
        articleMeta?: boolean;
        llmsFull?: boolean;
    };
    // (undocumented)
    siteDescription?: string;
    siteName?: string;
    siteUrl?: string;
}

// @public
export interface KestrelMediaConfig {
    // (undocumented)
    allowedMimes?: string;
    baseUrl?: string;
    // (undocumented)
    driver?: 'local' | 's3';
    image?: {
        widths?: number[];
        webpQuality?: number;
        jpegQuality?: number;
        variants?: VariantSpec[];
    };
    // (undocumented)
    maxBytes?: number;
    // (undocumented)
    s3?: KestrelS3Config;
    uploadDir?: string;
}

// @alpha
export interface KestrelPackageDiscovery {
    collections?: (CollectionDef | BuiltCollection)[];
    manifest?: OwnershipManifest;
    schemaTables?: AnySQLiteTable[];
}

// @public
export interface KestrelS3Config {
    bucket?: string;
    endpoint?: string;
    prefix?: string;
    publicBaseUrl?: string;
    region?: string;
}

// @public
export type KeyMode = 'columns' | 'props';

// @public
export function layerKestrelOption(config: KestrelConfig, moduleUrl: string): KestrelConfig;

// @public
export interface LayoutGroup {
    // (undocumented)
    kind: 'group';
    // (undocumented)
    label: Localized;
    // (undocumented)
    rows: LayoutRow[];
}

// @public (undocumented)
export type LayoutNode = LayoutRow | LayoutGroup;

// @public
export interface LayoutRow {
    // (undocumented)
    fields: string[];
    // (undocumented)
    kind: 'row';
    tracks: LayoutTrack[];
}

// @public
export type LayoutTrack = number | string;

// @public
export type LinkType = 'internal' | 'external' | 'email' | 'tel';

// @public
export type LinkValue = {
    type: 'internal';
    collection: string;
    id: number;
    hash?: string;
    label?: string;
} | {
    type: 'external';
    url: string;
    label?: string;
} | {
    type: 'email';
    email: string;
    label?: string;
} | {
    type: 'tel';
    tel: string;
    label?: string;
};

// @public (undocumented)
export function list(db: DB_2, c: BuiltCollection, q: ListQuery, publishedOnly?: boolean, publicOnly?: boolean): ListResult;

// @public (undocumented)
export interface ListQuery {
    capture?: boolean;
    // (undocumented)
    depth?: number;
    // (undocumented)
    filter?: FilterClause[];
    // (undocumented)
    locale?: string | string[];
    // (undocumented)
    page?: number;
    // (undocumented)
    perPage?: number;
    // (undocumented)
    sort?: string | string[];
    withTotal?: boolean;
}

// @public (undocumented)
export interface ListResult {
    // (undocumented)
    data: Record<string, unknown>[];
    // (undocumented)
    page: number;
    // (undocumented)
    perPage: number;
    quarantinedCount?: number;
    // (undocumented)
    total: number;
}

// @public (undocumented)
export type Locale = string;

// @public
export function localePath(path: string, locale: string, primaryLocale: string, prefixPrimary?: boolean): string;

// @public
export type Localized = string | Record<string, string>;

// @public
export interface LocatedDeadRef extends LocatedRef {
    // (undocumented)
    reason: DeadReason;
}

// @public
export interface LocatedRef extends FieldRef {
    blockId?: string;
    field: string;
}

// Warning: (ae-forgotten-export) The symbol "WriteDB" needs to be exported by the entry point index.d.ts
//
// @public
export function maintainRecordRefs(db: WriteDB, event: WriteEvent): void;

// @public
export function makeModuleDb(manifest: OwnershipManifest, sqlite: Database.Database, tables: Record<string, AnySQLiteTable>): {
    layer: Layer.Layer<ModuleDbService>;
    tag: Context.Tag<ModuleDbService, ModuleDbService>;
};

// @public
export function makeTicker(getDb: () => BetterSQLite3Database, module: string): () => Promise<PollResult | null>;

// @public
export const MAX_BULK_IDS = 500;

// @public
export const MAX_JSON_BODY: number;

// @public
export const MAX_PER_PAGE = 500;

// @public
export function mediaLockKey(storageKey: string): string;

// @public
export function memoDuringPrerender<A extends unknown[], R>(fn: (...args: A) => R, key: (...args: A) => string): (...args: A) => R;

// @public
export function memoResolver<A extends unknown[], R>(fn: (...args: A) => R, key: (...args: A) => string): (...args: A) => R | null;

// @alpha
export function mergeKestrelDiscovered<T>(packageItems: T[], layerItems: T[], nameOf: (item: T) => string): T[];

// @public
export const META_FILTER_KIND: Record<string, FilterKind>;

// @public
export const META_KEYS: readonly ["sitemap.xml", "robots.txt", "llms.txt", "llms-full.txt", "redirects.json"];

// @public
export function migrateRevisions(db: BetterSQLite3Database, opts: {
    force: boolean;
}): MigrateRevisionsResult[];

// @public (undocumented)
export interface MigrateRevisionsResult {
    // (undocumented)
    collection: string;
    seeded: number;
    skipped: number;
}

// @public
export const MODULE_MIGRATION_ORDER: readonly ["content", "media", "publishing"];

// @public
export const ModuleDbBrand: unique symbol;

// @public
export interface ModuleDbService {
    // (undocumented)
    readonly db: {
        readonly [ModuleDbBrand]: true;
        readonly select: BetterSQLite3Database['select'];
        readonly selectDistinct: BetterSQLite3Database['selectDistinct'];
        readonly insert: BetterSQLite3Database['insert'];
        readonly update: BetterSQLite3Database['update'];
        readonly delete: BetterSQLite3Database['delete'];
        readonly prepare: (sql: string) => ModuleStatement;
        readonly transaction: <T>(fn: (tx: ModuleTxHandle) => T, config?: ModuleTxConfig) => T;
    };
    // (undocumented)
    readonly tables: Record<string, AnySQLiteTable>;
}

// @public
export function nextSequence(sqlite: Database.Database, module: string, aggregateKey: string): number;

// @public
export function normalizeExemptPrefix(raw: string): string;

// @public
export const opAllowed: (kind: FilterKind, op: FilterOp) => boolean;

// @public (undocumented)
export const OPS_BY_KIND: Record<FilterKind, readonly FilterOp[]>;

// @public
export function opTable(op: SchemaOp): string;

// @public
export function orderManifests(manifests: readonly OwnershipManifest[]): OwnershipManifest[];

// @public (undocumented)
export const outboxContent: SQLiteTableWithColumns<    {
name: "outbox_content";
schema: undefined;
columns: {
id: SQLiteColumn<    {
name: "id";
tableName: "outbox_content";
dataType: "number";
columnType: "SQLiteInteger";
data: number;
driverParam: number;
notNull: true;
hasDefault: true;
isPrimaryKey: true;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: undefined;
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {}>;
envelope: SQLiteColumn<    {
name: "envelope";
tableName: "outbox_content";
dataType: "string";
columnType: "SQLiteText";
data: string;
driverParam: string;
notNull: true;
hasDefault: false;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: [string, ...string[]];
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {
length: number | undefined;
}>;
aggregateKey: SQLiteColumn<    {
name: "aggregate_key";
tableName: "outbox_content";
dataType: "string";
columnType: "SQLiteText";
data: string;
driverParam: string;
notNull: true;
hasDefault: false;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: [string, ...string[]];
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {
length: number | undefined;
}>;
sequence: SQLiteColumn<    {
name: "sequence";
tableName: "outbox_content";
dataType: "number";
columnType: "SQLiteInteger";
data: number;
driverParam: number;
notNull: true;
hasDefault: false;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: undefined;
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {}>;
processedAt: SQLiteColumn<    {
name: "processed_at";
tableName: "outbox_content";
dataType: "string";
columnType: "SQLiteText";
data: string;
driverParam: string;
notNull: false;
hasDefault: false;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: [string, ...string[]];
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {
length: number | undefined;
}>;
attempts: SQLiteColumn<    {
name: "attempts";
tableName: "outbox_content";
dataType: "number";
columnType: "SQLiteInteger";
data: number;
driverParam: number;
notNull: true;
hasDefault: true;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: undefined;
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {}>;
dead: SQLiteColumn<    {
name: "dead";
tableName: "outbox_content";
dataType: "number";
columnType: "SQLiteInteger";
data: number;
driverParam: number;
notNull: true;
hasDefault: true;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: undefined;
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {}>;
};
dialect: "sqlite";
}>;

// @public
export type OutboxHandler = (envelope: EventEnvelope) => Promise<void>;

// Warning: (ae-forgotten-export) The symbol "Registration" needs to be exported by the entry point index.d.ts
//
// @public
export function outboxHandlersFor(event: string): readonly Registration[];

// @public
export interface OutboxRow {
    // (undocumented)
    aggregateKey: string;
    // (undocumented)
    attempts: number;
    // (undocumented)
    dead: boolean;
    // (undocumented)
    envelope: EventEnvelope;
    // (undocumented)
    id: number;
    // (undocumented)
    processedAt: string | null;
    // (undocumented)
    sequence: number;
}

// @public
export function outboxTableName(module: string): string;

// Warning: (ae-forgotten-export) The symbol "OwnershipViolation_base" needs to be exported by the entry point index.d.ts
//
// @public
export class OwnershipViolation extends OwnershipViolation_base<{
    readonly module: string;
    readonly table: string;
}> {
}

// @public
export function pagePathTag(path: string): string;

// @public
export function pageRowHref(row: {
    path?: unknown;
    locale?: unknown;
} | null | undefined, primary: string, prefixPrimary?: boolean): string | null;

// @public
export function parseFilter(query: Record<string, unknown>): FilterClause[];

// @public (undocumented)
export function parseId(raw: string): number;

// @public
export function parseIdList(raw: unknown, cap: number): number[];

// @public
export function parseLocaleList(raw: string | undefined, fallback: string[]): string[];

// @public
export function parsePipelineRoute(path: string): PipelineRoute;

// @public (undocumented)
export function parseRecordId(segment: string): number;

// @public
export function parseRichtextLinkHref(href: string | null | undefined): {
    collection: string;
    id: number;
} | null;

// @public
export const PER_PAGE_OPTIONS: readonly [25, 50, 100, 250, 500];

// Warning: (ae-forgotten-export) The symbol "DB_3" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function pickerOptions(db: DB_3, c: BuiltCollection, q: PickerQuery): PickerResult;

// @public (undocumented)
export interface PickerQuery {
    // (undocumented)
    ids?: number[];
    // (undocumented)
    label?: string;
    // (undocumented)
    locale?: string;
    // (undocumented)
    page?: number;
    // (undocumented)
    perPage?: number;
    publishedOnly?: boolean;
    // (undocumented)
    search?: string;
}

// @public (undocumented)
export interface PickerResult {
    // (undocumented)
    data: {
        id: number;
        label: string;
    }[];
    // (undocumented)
    page: number;
    // (undocumented)
    perPage: number;
    // (undocumented)
    total: number;
}

// @public
export function pipelineAccess(collection: string | null, op: string): AccessSpec | undefined;

// @public
export interface PipelineContext<TIn = unknown, TOut = unknown> {
    // Warning: (ae-forgotten-export) The symbol "ExecPlane" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly exec: Readonly<ExecPlane>;
    // (undocumented)
    readonly facts: Readonly<RequestFacts>;
    // (undocumented)
    id?: number;
    // (undocumented)
    input: TIn;
    // (undocumented)
    output: TOut;
    // Warning: (ae-forgotten-export) The symbol "PipelinePorts" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly ports: Readonly<PipelinePorts>;
    // (undocumented)
    trace: TraceCollector;
    // (undocumented)
    work: Record<string, unknown>;
}

// Warning: (ae-forgotten-export) The symbol "GateSpec" needs to be exported by the entry point index.d.ts
//
// @public
export interface PipelineDef extends GateSpec {
    // Warning: (ae-forgotten-export) The symbol "AfterStepDef" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    after?: AfterStepDef[];
    // Warning: (ae-forgotten-export) The symbol "AnySchema" needs to be exported by the entry point index.d.ts
    input?: AnySchema;
    // (undocumented)
    name: string;
    // (undocumented)
    on?: {
        collection?: string;
    };
    op?: string;
    // (undocumented)
    output?: AnySchema;
    // Warning: (ae-forgotten-export) The symbol "PatchOp" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    patch?: PatchOp[];
    rawBody?: boolean;
    read?: boolean;
    steps?: StepDef[];
    // Warning: (ae-forgotten-export) The symbol "PipelineActionUi" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    ui?: PipelineActionUi;
}

// @public (undocumented)
export interface PipelineDescriptor {
    // (undocumented)
    after: {
        name: string;
        critical: boolean;
    }[];
    // (undocumented)
    collection: string | null;
    // (undocumented)
    gates: {
        access: AccessSpec | null;
        csrf: boolean;
        ipAllowlist: boolean;
    };
    // (undocumented)
    name: string;
    // (undocumented)
    route: {
        url: string;
        method: 'GET' | 'POST';
    };
    // (undocumented)
    schema: {
        input?: Record<string, unknown>;
        output?: Record<string, unknown>;
    };
    singleton: boolean | null;
    // (undocumented)
    steps: {
        name: string;
        sync: boolean;
        sealed: boolean;
        when: string | null;
    }[];
    ui?: PipelineActionUi;
}

// @public (undocumented)
export interface PipelineRoute {
    collection: string | null;
    // (undocumented)
    id?: number;
    // (undocumented)
    pipeline: string;
}

// @public (undocumented)
export interface PipelineTrace {
    // (undocumented)
    collection: string | null;
    // Warning: (ae-forgotten-export) The symbol "GateTraceEntry" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    gates: GateTraceEntry[];
    // (undocumented)
    ms: number;
    // (undocumented)
    op: string;
    // (undocumented)
    pipeline: string;
    // Warning: (ae-forgotten-export) The symbol "StepTraceEntry" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    steps: StepTraceEntry[];
}

// @public
export function planOps(db: SyncDb, desired: SchemaSnapshot, dialect?: Dialect): SchemaOp[];

// @public
export function pollOnce(db: BetterSQLite3Database, module: string): Promise<PollResult>;

// @public
export function pollOnceEffect(db: BetterSQLite3Database, module: string): Effect.Effect<PollResult>;

// @public (undocumented)
export interface PollResult {
    // (undocumented)
    deadLettered: number;
    // (undocumented)
    processed: number;
    skipped: number;
}

// @public
export interface PopulateCtx {
    // (undocumented)
    def: CollectionDef;
    // (undocumented)
    depth: number;
    // (undocumented)
    locale: string;
    // (undocumented)
    publicOnly?: boolean;
}

// @public (undocumented)
export function populateRow(row: Record<string, unknown>, ctx: PopulateCtx): Record<string, unknown>;

// @public (undocumented)
export type Populator = (row: Record<string, unknown>, ctx: PopulateCtx) => Record<string, unknown>;

// @public
export const postgres: Dialect;

// @public
export function precompressedEncoding(filename: string, siblingNames: string[]): 'br' | 'gzip' | undefined;

// @public
export function prefixPrimaryLocale(): boolean;

// @public (undocumented)
export function primaryLocale(): string;

// @public
export function pruneAllDueRevisions(db: BetterSQLite3Database, now: Date): number;

// @public
export function pruneRevisions(sqlite: Database.Database, collection: string, recordId: number, policy: RevisionRetentionPolicy, now: Date): number;

// @public
export interface PutOptions {
    cacheControl?: string;
    contentEncoding?: string;
}

// @public
export function putSingleton(db: DB_2, c: BuiltCollection, locale: string | undefined, body: unknown, opts?: UpdateOptions): Promise<Row_3>;

// @public
export function rawSqliteClientOf(db: object): Database.Database | undefined;

// @public
export function readDeadLetters(db: BetterSQLite3Database, module: string): OutboxRow[];

// @public (undocumented)
export function readIfUnmodifiedSince(event: H3Event): number | undefined;

// @public
export function readOutbox(db: BetterSQLite3Database, module: string): OutboxRow[];

// @public
export function readPendingOutbox(db: BetterSQLite3Database, module: string, limit: number): OutboxRow[];

// @public
export function readRevisions(db: BetterSQLite3Database, collection: string, recordId: number): RevisionRow[];

// @public
export function rebuildFromRevisions(db: BetterSQLite3Database, collection: BuiltCollection, recordId: number): Row | null;

// Warning: (ae-forgotten-export) The symbol "RebuildDB" needs to be exported by the entry point index.d.ts
//
// @public
export function rebuildRecordRefs(db: RebuildDB): void;

// @public
export function recordDeadRefs(db: DB_4, c: BuiltCollection, id: number): LocatedDeadRef[];

// @public (undocumented)
export const recordRefs: SQLiteTableWithColumns<    {
name: "record_refs";
schema: undefined;
columns: {
id: SQLiteColumn<    {
name: "id";
tableName: "record_refs";
dataType: "number";
columnType: "SQLiteInteger";
data: number;
driverParam: number;
notNull: true;
hasDefault: true;
isPrimaryKey: true;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: undefined;
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {}>;
sourceColl: SQLiteColumn<    {
name: "source_coll";
tableName: "record_refs";
dataType: "string";
columnType: "SQLiteText";
data: string;
driverParam: string;
notNull: true;
hasDefault: false;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: [string, ...string[]];
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {
length: number | undefined;
}>;
sourceId: SQLiteColumn<    {
name: "source_id";
tableName: "record_refs";
dataType: "number";
columnType: "SQLiteInteger";
data: number;
driverParam: number;
notNull: true;
hasDefault: false;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: undefined;
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {}>;
targetColl: SQLiteColumn<    {
name: "target_coll";
tableName: "record_refs";
dataType: "string";
columnType: "SQLiteText";
data: string;
driverParam: string;
notNull: true;
hasDefault: false;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: [string, ...string[]];
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {
length: number | undefined;
}>;
targetId: SQLiteColumn<    {
name: "target_id";
tableName: "record_refs";
dataType: "number";
columnType: "SQLiteInteger";
data: number;
driverParam: number;
notNull: true;
hasDefault: false;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: undefined;
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {}>;
createdAt: SQLiteColumn<    {
name: "created_at";
tableName: "record_refs";
dataType: "date";
columnType: "SQLiteTimestamp";
data: Date;
driverParam: number;
notNull: true;
hasDefault: true;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: true;
enumValues: undefined;
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {}>;
};
dialect: "sqlite";
}>;

// @public
export function recordTimeToRollback(file: string, durationMs: number): void;

// @public
export function refineConditionalRequired<T extends z.ZodTypeAny>(schema: T, fields: Record<string, FieldDef>): z.ZodTypeAny;

// Warning: (ae-forgotten-export) The symbol "GenId" needs to be exported by the entry point index.d.ts
//
// @public
export function regenerateBlockIds(content: unknown, genId?: GenId): unknown;

// @public
export function registerAfterStep(options: RegisterAfterStepOptions): void;

// @public (undocumented)
export interface RegisterAfterStepOptions {
    // (undocumented)
    critical: boolean;
    // (undocumented)
    on?: {
        collection?: string;
    };
    ops?: readonly string[];
    // (undocumented)
    step: StepDef;
}

// @public (undocumented)
export function registerBlock(def: BlockDef): void;

// @public (undocumented)
export function registerCollection(collection: BuiltCollection): void;

// Warning: (ae-forgotten-export) The symbol "DefaultsProvider" needs to be exported by the entry point index.d.ts
//
// @public
export function registerDefaultPipelines(defs: PipelineDef[] | DefaultsProvider): void;

// @public (undocumented)
export function registerFieldPopulator(type: string, fn: FieldPopulator): void;

// @public
export function registerFieldType(name: string, descriptor: FieldTypeDescriptor): void;

// @public
export function registerOutboxHandler(name: string, on: {
    event: string;
}, handler: OutboxHandler): void;

// @public (undocumented)
export function registerPipeline(def: PipelineDef): void;

// @public (undocumented)
export function registerPopulator(fn: Populator): void;

// @public
export function registerReindexRefs(): void;

// Warning: (ae-forgotten-export) The symbol "RevisionUpcastStep" needs to be exported by the entry point index.d.ts
//
// @public
export function registerRevisionUpcast(collection: string, fromVersion: number, step: RevisionUpcastStep): void;

// @public
export function remove(db: DB_2, c: BuiltCollection, id: number): {
    deleted: true;
    id: number;
};

// @public
export function removeMany(db: DB_2, c: BuiltCollection, ids: number[]): BatchResult;

// @public
export function renderDashboard(data: DashboardData): string;

// @public
export function renderSqlite(ops: SchemaOp[]): string[];

// @public
export function replayReadTags(tags: readonly string[]): void;

// @public
export const REQUEST_RESOLVE_BUDGET = 20000;

// @public (undocumented)
export function requireCollection(event: H3Event): BuiltCollection;

// @public (undocumented)
export function requireId(event: H3Event): number;

// @public
export function requireRecordId(ctx: PipelineContext): Effect.Effect<number, ValidationFailed>;

// @public
export function requireRegisteredCollection(name: string): BuiltCollection;

// @public
export function resetDbInstance(): void;

// @public (undocumented)
export function resolveBudgetFor(perPage?: number): number;

// @public (undocumented)
export function resolveColumnName(key: string, field: FieldDef): {
    jsKey: string;
    dbName: string;
};

// @public
export function resolveDefaultPipeline(collection: string | null, op: string): ResolvedPipeline;

// @public
export function resolveDialect(name: string): Dialect;

// @public
export interface ResolvedImagePolicy {
    jpegQuality: number;
    presets: ResolvedVariant[];
    variants: ResolvedVariant[];
    webpQuality: number;
    widths: number[];
}

// @public (undocumented)
export interface ResolvedKestrel {
    aiDisclosure: {
        enabled: boolean;
    };
    collections: {
        pages: boolean;
        media: boolean;
    };
    // (undocumented)
    dbPath: string;
    delivery: 'static' | 'live';
    deliveryExempt: string[];
    // (undocumented)
    media: {
        dir: string;
        baseUrl: string;
        driver: 'local' | 's3';
        maxUploadBytes: number;
        allowedMimes: string;
        s3: {
            bucket: string;
            region: string;
            endpoint: string;
            prefix: string;
            publicBaseUrl: string;
            accessKeyId?: string;
            secretAccessKey?: string;
            sessionToken?: string;
        };
        imagePolicy: ResolvedImagePolicy;
    };
    output: {
        driver: 'local' | 's3';
        dir: string;
        publicDir: string;
        auto: boolean;
        publishOnSave: boolean;
        reconcileMinutes: number;
        verbose: boolean;
        s3: ResolvedS3Settings;
    };
    // (undocumented)
    prefixPrimary: boolean;
    preview: {
        desktopWidth: number;
    };
    // (undocumented)
    primaryLocale: string;
    revisions: {
        keep: number | 'all';
        maxAgeDays?: number;
    };
    seo: {
        articleMeta: boolean;
        llmsFull: boolean;
    };
    // (undocumented)
    siteDescription: string;
    // (undocumented)
    siteName: string;
    // (undocumented)
    siteUrl: string;
    // (undocumented)
    supportedLocales: string[];
}

// @public
export interface ResolvedS3Settings {
    // (undocumented)
    bucket: string;
    // (undocumented)
    endpoint: string;
    // (undocumented)
    prefix: string;
    // (undocumented)
    region: string;
}

// @public
export interface ResolvedVariant {
    // (undocumented)
    fit: VariantFit;
    // (undocumented)
    formats: VariantFormat[];
    height: number | null;
    // (undocumented)
    name: string;
    // (undocumented)
    position: string;
    // (undocumented)
    width: number;
}

// @public
export function resolveFieldLayout(dsl: FieldLayoutDSL, fieldKeys: string[], ctx: string): LayoutNode[];

// Warning: (ae-forgotten-export) The symbol "Env" needs to be exported by the entry point index.d.ts
//
// @public
export function resolveKestrel(config: KestrelConfig | undefined, env: Env, rootDir: string): ResolvedKestrel;

// @public
export function resolveLocale(value?: string | string[] | null): Locale;

// @public
export function resolveRichtextLinks(html: string | null | undefined, resolveHref: (collection: string, id: number) => string | null): string;

// @public
export function resolveS3Settings(cfg: {
    bucket?: string;
    region?: string;
    endpoint?: string;
    prefix?: string;
} | undefined, env: Env, envPrefix: string): ResolvedS3Settings;

// @public
export function resolveTranslations(db: BetterSQLite3Database, c: BuiltCollection, id: number): Record<string, number | null>;

// @public
export function resolveVariants(cfg: VariantSpec[] | undefined, cfgWidths: number[] | undefined, envWidths: string | undefined): ResolvedVariant[];

// @public (undocumented)
export interface RevisionRetentionPolicy {
    // (undocumented)
    keep: number | 'all';
    // (undocumented)
    maxAgeDays?: number;
}

// @public
export function revisionRetentionPolicy(_collection: string): ResolvedKestrel['revisions'];

// @public
export interface RevisionRow {
    // (undocumented)
    correlationId: string;
    // (undocumented)
    createdAt: string;
    // (undocumented)
    recordId: number;
    // (undocumented)
    revision: number;
    // (undocumented)
    schemaVersion: number;
    // (undocumented)
    snapshot: Row | null;
    // (undocumented)
    tombstone: boolean;
}

// @public
export function revisionsTable(collection: string): AnySQLiteTable;

// @public
export function revisionsTableName(collection: string): string;

// @public
export interface RevisionUpcastOutcome {
    // (undocumented)
    resolved: boolean;
    // (undocumented)
    snapshot: Row;
}

// @public (undocumented)
export const RICHTEXT_ALLOWLIST: sanitizeHtml.IOptions;

// @public
export const RICHTEXT_LINK_SCHEME = "kestrel";

// @public
export function richtextLinkHref(collection: string, id: number): string;

// @public (undocumented)
export interface RouteConflict {
    // (undocumented)
    collection: string;
    // (undocumented)
    id: number;
}

// @public
export function routeOf(row: {
    path?: unknown;
    locale?: unknown;
}, translatable: boolean, primary: string, prefixPrimary?: boolean): string | null;

// @public (undocumented)
export type Row = Record<string, unknown>;

// @public
export function runDevSchemaSync(client: SyncDb, desired: SchemaSnapshot, dialect?: Dialect): void;

// @public (undocumented)
export function runPipeline<TOut>(resolved: ResolvedPipeline, ctx: PipelineContext<unknown, TOut>, evaluators?: Partial<GateEvaluators>): Promise<TOut>;

// @public
export function runPipelineSync<TOut>(resolved: ResolvedPipeline, ctx: PipelineContext<unknown, TOut>, evaluators?: Partial<GateEvaluators>): TOut;

// @public
export function runRead<TOut>(op: string, options: Omit<PipelineContextOptions, 'op'>): TOut;

// @public
export function runWrite<TOut>(op: string, options: Omit<PipelineContextOptions, 'op'>): TOut;

// @public
export function runWriteAfterStepsSync(op: string, collection: BuiltCollection, event: WriteEvent): void;

// @public
export interface S3DriverOptions {
    // (undocumented)
    accessKeyId: string;
    // (undocumented)
    bucket: string;
    endpoint?: string;
    prefix?: string;
    publicBaseUrl: string;
    region?: string;
    // (undocumented)
    secretAccessKey: string;
    // (undocumented)
    sessionToken?: string;
}

// @public
export function sanitizeRichtext(html: string): SanitizedRichtext;

// @public
export type SchemaOp = {
    type: 'create_table';
    table: TableShape;
} | {
    type: 'add_column';
    table: string;
    column: ColumnShape;
} | {
    type: 'create_index';
    index: IndexShape;
} | {
    type: 'drop_index';
    index: IndexShape;
} | {
    type: 'rename_column';
    table: string;
    from: string;
    to: string;
} | {
    type: 'drop_table';
    name: string;
} | {
    type: 'rebuild_table';
    table: TableShape;
    copy: string[];
    transforms?: Record<string, {
        from: string;
        type: 'wrap' | 'unwrap';
    }>;
};

// @public
export type SchemaSnapshot = Record<string, TableShape>;

// @public (undocumented)
export function schemaVersionOf(def: CollectionDef): number;

// @public
export function seedBuiltinFieldTypes(entries: Record<string, FieldTypeDescriptor>): Record<string, FieldTypeDescriptor>;

// @public (undocumented)
export interface SeoMeta {
    author?: string;
    // (undocumented)
    description?: string;
    image?: number | null;
    keywords?: string;
    // (undocumented)
    noindex?: boolean;
    publishedDate?: string;
    // (undocumented)
    title?: string;
}

// @public (undocumented)
export const seoPopulateFields: Record<string, FieldDef>;

// @public (undocumented)
export const seoSchema: z.ZodObject<{
    title: z.ZodOptional<z.ZodString>;
    description: z.ZodOptional<z.ZodString>;
    noindex: z.ZodOptional<z.ZodBoolean>;
    image: z.ZodOptional<z.ZodNullable<z.ZodNumber>>;
    author: z.ZodOptional<z.ZodString>;
    publishedDate: z.ZodOptional<z.ZodString>;
    keywords: z.ZodOptional<z.ZodString>;
}, z.core.$strip>;

// @public
export function serializeBlock(def: {
    name: string;
    label?: Localized;
    slots?: string[];
    icon?: string;
    image?: string;
    fields: Record<string, FieldDef>;
}): SerializedBlock;

// @public
export function serializeCollection(def: CollectionDef, actions?: SerializedAction[]): SerializedCollection;

// @public (undocumented)
export interface SerializedAction {
    // (undocumented)
    confirm?: boolean;
    // (undocumented)
    icon?: string;
    // (undocumented)
    kind: 'bulk' | 'record' | 'both';
    // (undocumented)
    label?: Localized;
    // (undocumented)
    name: string;
    // (undocumented)
    route: {
        url: string;
        method: 'GET' | 'POST';
    };
}

// @public
export interface SerializedBlock {
    // (undocumented)
    fields: Record<string, SerializedField>;
    icon?: string;
    image?: string;
    // (undocumented)
    label?: Localized;
    // (undocumented)
    name: string;
    // (undocumented)
    slots?: string[];
}

// @public
export interface SerializedCollection {
    actions?: SerializedAction[];
    // (undocumented)
    blocks: {
        enabled: boolean;
        allowed?: string[];
    };
    editor: string;
    fieldLayout?: LayoutNode[];
    // (undocumented)
    fields: Record<string, SerializedField>;
    // (undocumented)
    icon?: string;
    // (undocumented)
    label?: {
        singular?: Localized;
        plural?: Localized;
        new?: Localized;
    };
    // (undocumented)
    mode: 'multi' | 'single';
    // (undocumented)
    name: string;
    nav: boolean;
    // (undocumented)
    pageLike: boolean;
    // (undocumented)
    seo: boolean;
    // (undocumented)
    status: boolean;
    // (undocumented)
    translatable: boolean;
}

// @public
export interface SerializedField {
    condition?: Condition;
    // (undocumented)
    default?: unknown;
    label?: Localized;
    // (undocumented)
    options?: Record<string, unknown>;
    // (undocumented)
    relation?: {
        collection: string;
        many: boolean;
        labelField?: string;
    };
    // (undocumented)
    required: boolean;
    single?: boolean;
    // (undocumented)
    type: FieldType;
    // (undocumented)
    unique: boolean;
}

// @public
export function serializeField(field: FieldDef): SerializedField;

// @public
export function serializeFields(fields: Record<string, FieldDef>): Record<string, SerializedField>;

// @public
export function setResolvedKestrelConfig(cfg: ResolvedKestrel): void;

// @public
export function setStatusMany(db: DB_2, c: BuiltCollection, ids: number[], status: 'draft' | 'published'): BatchResult;

// @public
export function slugify(input: string): string;

// @public
export const sqlite: Dialect;

// @public
export function sqliteClientOf(db: BetterSQLite3Database): Database.Database;

// @public (undocumented)
export type Status = 'draft' | 'published';

// @public
export interface StepDef {
    // @internal
    [SYNC_BRAND]?: true;
    // (undocumented)
    fn: StepFn;
    // (undocumented)
    name: string;
    sealed?: boolean;
    sync?: boolean;
    // (undocumented)
    when?: (ctx: PipelineContext) => boolean;
    whenLabel?: string;
}

// @public
export interface StorageDriver {
    // (undocumented)
    copy(srcKey: string, dstKey: string): Promise<void>;
    // (undocumented)
    delete(key: string, opts?: DeleteOptions): Promise<void>;
    // (undocumented)
    ensureDir?(folder: string): Promise<void>;
    // (undocumented)
    exists?(key: string): Promise<boolean>;
    get?(key: string): Promise<Buffer>;
    list?(): Promise<string[]>;
    listPrefix?(prefix: string): Promise<string[]>;
    // (undocumented)
    publicUrl(key: string): string;
    // (undocumented)
    put(key: string, bytes: Buffer | Uint8Array, contentType: string, opts?: PutOptions): Promise<void>;
    // (undocumented)
    removeDir?(folder: string): Promise<void>;
    stat?(key: string): Promise<{
        mtimeMs: number | null;
    } | null>;
}

// @public (undocumented)
export function supportedLocales(): readonly string[];

// @public (undocumented)
export interface SyncDb extends IntrospectDb {
    // (undocumented)
    exec(sql: string): unknown;
    // (undocumented)
    transaction(fn: () => void): () => void;
}

// @public (undocumented)
export interface SyncOptions {
    allowDestructive?: boolean;
    dropTables?: string[];
    tables?: readonly string[];
}

// @public (undocumented)
export interface SyncResult {
    applied: string[];
    skipped: SchemaOp[];
}

// @public
export function syncSchema(db: SyncDb, desired: SchemaSnapshot, opts?: SyncOptions, dialect?: Dialect): SyncResult;

// @public
export function syncStep(name: string, fn: StepFn, options?: StepOptions): StepDef;

// @public (undocumented)
export interface TableShape {
    // (undocumented)
    columns: ColumnShape[];
    // (undocumented)
    indexes: IndexShape[];
    // (undocumented)
    name: string;
}

// @public
export function toHttpError(error: unknown): unknown;

// @public
export class TraceCollector {
    // Warning: (ae-forgotten-export) The symbol "TraceMeta" needs to be exported by the entry point index.d.ts
    constructor(meta: TraceMeta);
    annotate(key: string, value: unknown): void;
    // Warning: (ae-forgotten-export) The symbol "StepPhase" needs to be exported by the entry point index.d.ts
    // Warning: (ae-forgotten-export) The symbol "StepStatus" needs to be exported by the entry point index.d.ts
    beginStep(name: string, phase: StepPhase, critical?: boolean): (status: StepStatus, reason?: string) => void;
    // (undocumented)
    gate(gate: string, passed: boolean, detail?: string): void;
    // (undocumented)
    toJSON(): PipelineTrace;
}

// @public (undocumented)
export interface TransitionRow {
    // (undocumented)
    from: Status;
    // (undocumented)
    guard?: GuardName;
    // (undocumented)
    to: Status;
}

// @public
export const transitions: ReadonlyArray<TransitionRow>;

// @public
export function translationGroupTag(coll: string, group: string): string;

// @public
export function tryResolveDefaultPipeline(collection: string | null, op: string): ResolvedPipeline | undefined;

// @public (undocumented)
export function update(db: DB_2, c: BuiltCollection, id: number, body: unknown, opts?: UpdateOptions): Row_3;

// @public (undocumented)
export interface UpdateOptions {
    expectedUpdatedAt?: number;
}

// @public
export function useContentDb(): ModuleDbService;

// @public
export function useContentDbFor(db: BetterSQLite3Database, extra?: BuiltCollection): ModuleDbService;

// @public (undocumented)
export function useDb(): BetterSQLite3Database;

// @public
export function validateFieldLayoutsDeep(def: CollectionDef): void;

// @public
export type VariantFit = 'cover' | 'contain' | 'inside' | 'outside' | 'fill';

// @public
export type VariantFormat = 'webp' | 'jpeg';

// @public
export interface VariantSpec {
    fit?: VariantFit;
    formats?: VariantFormat[];
    height?: number;
    name: string;
    position?: string;
    width: number;
}

// @public
export function withLock<T>(key: string, fn: () => Promise<T>): Promise<T>;

// @public
export function withReadCapture<T>(fn: () => Promise<T> | T): Promise<{
    result: T;
    tags: string[];
}>;

// @public
export function withReadCaptureSync<T>(fn: () => T): {
    result: T;
    tags: string[];
};

// @public (undocumented)
export function withResolveScope<T>(fn: () => T, max?: number, label?: string): T;

// @public
export const WRITE_OPS: readonly ["createOne", "createMany", "updateOne", "updateMany", "deleteOne", "deleteMany"];

// @public
export interface WriteEvent {
    // (undocumented)
    after: Row_2 | null;
    // (undocumented)
    before: Row_2 | null;
    // (undocumented)
    def: CollectionDef;
}

// Warnings were encountered during analysis:
//
// src/server/db/module-db.ts:109:5 - (ae-forgotten-export) The symbol "ModuleStatement" needs to be exported by the entry point index.d.ts
// src/server/db/module-db.ts:110:5 - (ae-forgotten-export) The symbol "ModuleTxHandle" needs to be exported by the entry point index.d.ts
// src/server/db/module-db.ts:110:5 - (ae-forgotten-export) The symbol "ModuleTxConfig" needs to be exported by the entry point index.d.ts
// src/server/schema/introspect.ts:11:27 - (ae-forgotten-export) The symbol "Row_4" needs to be exported by the entry point index.d.ts

```
