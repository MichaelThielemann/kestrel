## API Report File for "@michaelthielemann/kestrel-delivery-static"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { DeliveryPort } from '@michaelthielemann/kestrel-contracts';
import type { StorageDriver } from '@michaelthielemann/kestrel-core';

// @public
export function createStaticDeliveryPort(driver: StorageDriver): DeliveryPort;

// @public
export function deliveryPortFor(delivery: 'static' | 'live', driver: StorageDriver): DeliveryPort;

// @public
export type DeployDriver = Pick<StorageDriver, 'put'> & Partial<Pick<StorageDriver, 'list' | 'delete'>>;

// @public (undocumented)
export interface DeployOptions {
    concurrency?: number;
    // (undocumented)
    dryRun?: boolean;
    incomplete?: string;
    // (undocumented)
    log?: (msg: string) => void;
    retries?: number;
    sleep?: (ms: number) => Promise<void>;
}

// @public (undocumented)
export interface DeployResult {
    // (undocumented)
    keys: string[];
    // (undocumented)
    pruned: number;
}

// @public
export function deployStaticOutput(dir: string, driver: DeployDriver, opts?: DeployOptions): Promise<DeployResult>;

// @public
export function isCompressedSidecar(key: string, all: Set<string>): boolean;

// @public
export function isEnvTrue(value: string | undefined): boolean;

// @public
export function isPermanentError(err: unknown): boolean;

// @public (undocumented)
export type OutputConfig = {
    driver?: string;
    s3?: {
        bucket?: string;
        region?: string;
        endpoint?: string;
        prefix?: string;
    };
};

// @public (undocumented)
export interface OutputTarget {
    // (undocumented)
    driver: 'local' | 's3';
    s3: {
        bucket: string;
        region: string;
        endpoint: string;
        prefix: string;
    };
}

// @public (undocumented)
export function planS3Deploy(opts: {
    isStaticGenerate: boolean;
    dryRun: boolean;
    bucket: string;
    accessKeyId: string;
    secretAccessKey: string;
    prefix?: string;
    autoPublish?: boolean;
    mediaBucket?: string;
    mediaPrefix?: string;
}): 'skip' | 'deploy';

// @public
export function readRouteDiscovery(nuxt: object): RouteDiscovery | undefined;

// @public
export function recordRouteDiscovery(nuxt: object, discovery: RouteDiscovery): void;

// @public
export function renderRoute(route: string): {
    body: Buffer | null;
    status: number;
};

// @public
export function resolveOutputCreds(env?: Record<string, string | undefined>): {
    accessKeyId: string;
    secretAccessKey: string;
    sessionToken?: string;
};

// @public
export function resolveOutputTarget(out?: OutputConfig, env?: Record<string, string | undefined>): OutputTarget;

// @public (undocumented)
export interface RouteDiscovery {
    // (undocumented)
    incomplete?: string;
    // (undocumented)
    routes: string[];
}

// (No @packageDocumentation comment for this package)

```
