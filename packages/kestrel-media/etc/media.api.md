## API Report File for "@kestrel/media"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { BetterSQLite3Database } from 'drizzle-orm/better-sqlite3';
import { BuiltCollection } from '@kestrel/core';
import type Database from 'better-sqlite3';
import type { FieldPopulator } from '@kestrel/core';
import { H3Event } from 'h3';
import type { KestrelPackageDiscovery } from '@kestrel/core';
import { ModuleDbService } from '@kestrel/core';
import type { OwnershipManifest } from '@kestrel/contracts';
import type { PipelineDef } from '@kestrel/core';
import type { ResolvedImagePolicy } from '@kestrel/core';
import type { ResolvedVariant } from '@kestrel/core';
import { SQLiteColumn } from 'drizzle-orm/sqlite-core';
import { SQLiteTable } from 'drizzle-orm/sqlite-core';
import { SQLiteTableWithColumns } from 'drizzle-orm/sqlite-core';
import type { StorageDriver } from '@kestrel/core';
import { TableConfig } from 'drizzle-orm/sqlite-core';
import type { VariantFit } from '@kestrel/core';

// @public
export interface AffectedMedia {
    // Warning: (ae-forgotten-export) The symbol "DerivativeManifest" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    derivatives: DerivativeManifest | null;
    // (undocumented)
    filename: string;
    // (undocumented)
    folder: string | null;
    // (undocumented)
    id: number;
    raw: Record<string, unknown>;
    // (undocumented)
    size: number;
    // (undocumented)
    storageKey: string;
}

// @public
export interface AffectedSet {
    // (undocumented)
    folders: string[];
    // (undocumented)
    media: AffectedMedia[];
}

// @public
export type AiSourceType = 'trainedAlgorithmicMedia' | 'compositeWithTrainedAlgorithmicMedia' | 'algorithmicallyEnhanced';

// @public
export interface BackfillPlan {
    missing: ResolvedVariant[];
    orphanKeys: string[];
}

// @public
export interface BackfillReport {
    // (undocumented)
    check: boolean;
    // (undocumented)
    generated: number;
    // (undocumented)
    pruned: number;
    pruneWithheld: boolean;
    // (undocumented)
    rows: number;
    // (undocumented)
    rowsChanged: number;
}

// Warning: (ae-forgotten-export) The symbol "BackfillRow" needs to be exported by the entry point index.d.ts
//
// @public
export function backfillRow(db: MediaDb, driver: StorageDriver, row: BackfillRow, specs: ResolvedVariant[], policy: ResolvedImagePolicy, prune?: boolean): Promise<{
    generated: number;
    pruned: number;
}>;

// @public
export function buildMediaFieldPopulator(resolveById: ResolveById): FieldPopulator;

// @public
export function buildMediaPipelines(): PipelineDef[];

// @public
export function bulkUsages(db: BetterSQLite3Database, ids: number[]): Record<number, MediaUsage[]>;

// @public
export function clearVariants(): void;

// @public
export function coerceOpItems(raw: unknown): OpItem[];

// @public
export function collectAffected(db: MediaDb, items: OpItem[]): AffectedSet;

// @public
export function collectVariants(): ResolvedVariant[];

// @public
export interface Conflict {
    // (undocumented)
    item: OpItem;
    // (undocumented)
    targetPath: string;
    // (undocumented)
    type: 'file-exists' | 'folder-exists';
}

// @public
export function deleteAffected(db: MediaDb, driver: StorageDriver, items: OpItem[]): Promise<DeleteReport>;

// @public
export interface DeleteReport {
    failedKeys?: string[];
    // (undocumented)
    summary: {
        files: number;
        folders: number;
        totalBytes: number;
    };
    // (undocumented)
    usages?: Record<number, MediaUsage[]>;
}

// @public
export function deriveOnDemand(db: MediaDb, driver: StorageDriver, policy: ResolvedImagePolicy, requestedKey: string): Promise<{
    bytes: Buffer;
    mime: string;
} | null>;

// @public
export function duplicateMedia(db: MediaDb, driver: StorageDriver, id: number, target: {
    folder?: string;
    filename?: string;
}): Promise<{
    id: number;
}>;

// @public
export function ensureFolder(db: MediaDb, path: string): void;

// @public
export function executeRelocation(db: MediaDb, driver: StorageDriver, op: MediaOp, onConflict?: 'abort' | 'skip' | 'overwrite' | 'rename'): Promise<OpResult[]>;

// @public
export function findMediaUsages(db: BetterSQLite3Database, id: number): MediaUsage[];

// @public
export function findMediaUsagesForMany(db: BetterSQLite3Database, ids: number[]): Record<number, MediaUsage[]>;

// @public
export const folders: SQLiteTableWithColumns<    {
name: "folders";
schema: undefined;
columns: {
id: SQLiteColumn<    {
name: "id";
tableName: "folders";
dataType: "number";
columnType: "SQLiteInteger";
data: number;
driverParam: number;
notNull: true;
hasDefault: true;
isPrimaryKey: true;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: undefined;
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {}>;
path: SQLiteColumn<    {
name: "path";
tableName: "folders";
dataType: "string";
columnType: "SQLiteText";
data: string;
driverParam: string;
notNull: true;
hasDefault: false;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: false;
enumValues: [string, ...string[]];
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {
length: number | undefined;
}>;
createdAt: SQLiteColumn<    {
name: "created_at";
tableName: "folders";
dataType: "date";
columnType: "SQLiteTimestamp";
data: Date;
driverParam: number;
notNull: true;
hasDefault: true;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: true;
enumValues: undefined;
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {}>;
updatedAt: SQLiteColumn<    {
name: "updated_at";
tableName: "folders";
dataType: "date";
columnType: "SQLiteTimestamp";
data: Date;
driverParam: number;
notNull: true;
hasDefault: true;
isPrimaryKey: false;
isAutoincrement: false;
hasRuntimeDefault: true;
enumValues: undefined;
baseColumn: never;
identity: undefined;
generated: undefined;
}, {}, {}>;
};
dialect: "sqlite";
}>;

// @alpha
export const kestrelDiscovery: KestrelPackageDiscovery;

// @public
export interface LibraryFolder {
    // (undocumented)
    name: string;
    // (undocumented)
    path: string;
    // (undocumented)
    size: number;
}

// @public
export interface LibraryQuery {
    // (undocumented)
    folder?: string;
    // (undocumented)
    page?: number;
    // (undocumented)
    perPage?: number;
    // (undocumented)
    search?: string;
    // (undocumented)
    sort?: string;
    // (undocumented)
    type?: 'image' | 'all';
}

// @public
export function listLibrary(db: MediaDb, q: LibraryQuery, publicUrl: (key: string) => string): {
    folder: string;
    exists: boolean;
    folders: LibraryFolder[];
    files: {
        id: number;
        filename: string;
        mime: string;
        folder: string;
        size: number;
        width: number | null;
        height: number | null;
        thumbhash: string | null;
        src: string;
        srcset: string | undefined;
        alt: string | null;
        aiDisclosure: {
            sourceType: AiSourceType;
            note: string | null;
        } | null;
        createdAt: Date;
        updatedAt: Date;
    }[];
    total: number;
    page: number;
    perPage: number;
};

// @public
export const media: SQLiteTable<TableConfig>;

// @public
export const mediaCollection: BuiltCollection;

// @public
export function mediaCollectionEnabled(): boolean;

// @public
export type MediaDb = ModuleDbService['db'];

// @public
export interface MediaOp {
    // (undocumented)
    dest?: string;
    // (undocumented)
    items: OpItem[];
    // (undocumented)
    name?: string;
    // (undocumented)
    type: OpType;
}

// @public
export const mediaOwnershipManifest: OwnershipManifest;

// @public
export interface MediaRuntimeConfig {
    // (undocumented)
    allowedMimes: string;
    // (undocumented)
    baseUrl: string;
    // (undocumented)
    dir: string;
    // (undocumented)
    driver: 'local' | 's3';
    // (undocumented)
    imagePolicy: ResolvedImagePolicy;
    // (undocumented)
    maxUploadBytes: number;
    // (undocumented)
    s3: {
        bucket: string;
        region: string;
        endpoint: string;
        prefix: string;
        publicBaseUrl: string;
        accessKeyId?: string;
        secretAccessKey?: string;
        sessionToken?: string;
    };
}

// @public
export function mediaRuntimeConfig(): MediaRuntimeConfig;

// @public
export const mediaSettings: SQLiteTable<TableConfig>;

// @public
export const mediaSettingsCollection: BuiltCollection;

// @public
export interface MediaUsage {
    // (undocumented)
    collection: string;
    // (undocumented)
    field: string;
    // (undocumented)
    recordId: number;
}

// @public
export interface MediaVariant {
    // (undocumented)
    format: string;
    // (undocumented)
    height: number;
    // (undocumented)
    name: string;
    // (undocumented)
    url: string;
    // (undocumented)
    width: number;
}

// @public
export type OpItem = {
    type: 'file';
    id: number;
} | {
    type: 'folder';
    path: string;
};

// @public
export interface OpResult {
    // (undocumented)
    item: OpItem;
    // (undocumented)
    newPath?: string;
    // (undocumented)
    status: 'moved' | 'copied' | 'renamed' | 'skipped' | 'overwritten' | 'renamed-auto';
}

// @public
export type OpType = 'move' | 'copy' | 'rename';

// @public
export function orderById<T extends {
    id: number;
}>(ids: number[], rows: T[]): T[];

// @public
export function parseVariantName(name: string): {
    width: number;
    height: number | null;
    fit: VariantFit;
} | null;

// @public
export function planBackfill(row: {
    width: number | null;
    derivatives: DerivativeManifest | null;
}, specs: ResolvedVariant[], prune?: boolean): BackfillPlan;

// @public
export function planItem(db: MediaDb, op: MediaOp, item: OpItem): {
    affected: AffectedSet;
    plan: ItemPlan;
};

// @public
export function planObjectRelocation(row: {
    storageKey: string;
    derivatives: DerivativeManifest | null;
}, folder: string, filename: string): RelocatePlan;

// @public
export function previewDelete(db: MediaDb, usagesDb: BetterSQLite3Database, items: OpItem[]): DeleteReport;

// @public
export function previewRelocation(db: MediaDb, op: MediaOp): RelocationReport;

// @public
export function recordVariants(specs: readonly ResolvedVariant[]): void;

// @public
export function registerMediaCleanup(): void;

// @public
export function relocateMedia(db: MediaDb, driver: StorageDriver, id: number, target: {
    folder?: string;
    filename?: string;
}): Promise<void>;

// @public
export interface RelocatePlan {
    // (undocumented)
    copies: {
        src: string;
        dst: string;
    }[];
    // (undocumented)
    derivatives: DerivativeManifest;
    // (undocumented)
    storageKey: string;
}

// @public
export interface RelocationReport {
    // (undocumented)
    conflicts: Conflict[];
    // (undocumented)
    summary: {
        files: number;
        folders: number;
        totalBytes: number;
    };
}

// @public
export function requireMediaCollection(): void;

// @public
export type ResolveById = (id: number, locale: string) => ResolvedMedia | null;

// @public
export interface ResolvedMedia {
    aiDisclosure: {
        sourceType: AiSourceType;
        note: string | null;
    } | null;
    // (undocumented)
    alt: string | null;
    // (undocumented)
    description: string | null;
    folder: string;
    // (undocumented)
    height: number | null;
    // (undocumented)
    id: number;
    // (undocumented)
    mime: string;
    // (undocumented)
    src: string;
    srcset: {
        url: string;
        width: number;
    }[];
    // (undocumented)
    thumbhash: string | null;
    // (undocumented)
    title: string | null;
    variants: MediaVariant[];
    // (undocumented)
    width: number | null;
}

// @public
export function resolveManyByIds(db: MediaDb, ids: number[], locale: string, publicUrl: (key: string) => string): ResolvedMedia[];

// Warning: (ae-forgotten-export) The symbol "MediaRow" needs to be exported by the entry point index.d.ts
//
// @public
export function resolveMedia(row: MediaRow, locale: string, publicUrl: (key: string) => string): ResolvedMedia;

// @public
export function resolveVariantRequest(db: MediaDb, requestedKey: string): VariantRequest | null;

// @public
export function runBackfill(db: MediaDb, driver: StorageDriver, policy: ResolvedImagePolicy, opts?: {
    check?: boolean;
}): Promise<BackfillReport>;

// @public
export function runRelocation(event: H3Event, buildOp: (items: OpItem[], body: Record<string, unknown>) => MediaOp): Promise<RelocationReport | OpResult[]>;

// @public
export function saveDiscoveredVariants(db: MediaDb | BetterSQLite3Database, opts?: {
    clear?: boolean;
}): void;

// @public
export function sqliteClientOfMediaDb(db: MediaDb): Database.Database;

// @public
export function useMediaDb(): ModuleDbService;

// @public
export function useMediaDbFor(db: BetterSQLite3Database): ModuleDbService;

// @public
export function useStorageDriver(): StorageDriver;

// @public
export function validateOp(op: MediaOp): void;

// @public
export function variantKeyFromPath(path: string, baseUrl: string | undefined): string | null;

// @public
export interface VariantRequest {
    // (undocumented)
    format: string;
    // (undocumented)
    mime: string;
    // (undocumented)
    name: string;
    // (undocumented)
    storageKey: string;
}

// Warnings were encountered during analysis:
//
// src/server/utils/relocate-ops.ts:57:92 - (ae-forgotten-export) The symbol "ItemPlan" needs to be exported by the entry point index.d.ts

// (No @packageDocumentation comment for this package)

```
