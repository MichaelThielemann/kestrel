# Dependencies
node_modules

# Nuxt / Nitro build output
.nuxt
.nuxtrc
.output
.nitro
.cache
dist

# Environment
.env
.env.*
!.env.example

# Runtime database (migrations under server/database/migrations are committed)
.data
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store

# Packaged tarballs (npm pack / release artifacts)
*.tgz

# Generated API reference (ADR-0015)
docs/api

# Generated introspection dashboard (pnpm dashboard)
docs/dashboard.html

# StrykerJS mutation-testing output
.stryker-tmp
reports/
docs/parity/candidates.md
docs/parity/candidates.json
docs/parity/agenda.json
docs/parity/mutants.json
docs/parity/verdicts.jsonl
.audit-history/

# api-extractor's scratch diff output (the accepted report lives in etc/*.api.md, committed)
packages/*/temp

# create-kestrel's payload — generated from the engine's own copies at pack time
packages/create-kestrel/templates
packages/create-kestrel/lib
packages/create-kestrel/LICENSE
packages/create-kestrel/NOTICE

