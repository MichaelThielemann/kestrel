import { publishStatus } from '../database/publish-status'

// Registers the standalone publish_status table with the schema engine (the `#kestrel/schema-tables` virtual).
export default publishStatus
